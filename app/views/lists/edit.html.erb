<h1>Comentário sobre a lista</h1>

<%= form_with model: @list do |f| %>

  <div>
    <%= f.label :comment %>
    <%= f.text_area :comment %>
  </div>

  <%= f.submit "Salvar comentário" %>

<% end %>
